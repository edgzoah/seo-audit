# PostgreSQL connection for Next.js API and CLI DB write path
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/seo_audit?schema=public"

# Optional: log DB query timings for audits repository
LOG_AUDIT_QUERY="0"
